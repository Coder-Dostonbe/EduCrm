# Where the frontend gets its data:
#   mock → bundled demo data, no backend needed (default)
#   api  → the Django REST API below
NEXT_PUBLIC_API_MODE=mock
NEXT_PUBLIC_API_URL=http://localhost:8000

# Google Sign-In (only needed when NEXT_PUBLIC_API_MODE=api)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
